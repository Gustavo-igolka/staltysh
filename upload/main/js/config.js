// Вставь сюда данные своего проекта Supabase (Project Settings → API).
// anon-ключ публичный, его безопасно держать в коде: защита данных — в правилах RLS (см. supabase/schema.sql).
// Пока поля пустые, игра работает локально (сохранение в браузере), без входа и рейтинга.
export const CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
